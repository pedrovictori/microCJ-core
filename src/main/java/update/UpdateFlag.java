package update;

/**
 * This enum is made up of flags that are expected to be placed in a queue populated by the model so that the viewer can look into it and update the graphical user interface as needed
 */
public enum UpdateFlag{
	DEAD_CELL,
	NECROTIC_CELL,
	NEW_CELL;
}
